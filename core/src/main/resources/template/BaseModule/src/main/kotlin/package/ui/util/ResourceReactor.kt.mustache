package {{packageName}}.ui.util

import androidx.compose.runtime.Composable
import {{packageName}}.ui.component.ErrorPage
import {{packageName}}.ui.component.InitPage
import {{packageName}}.ui.component.LoadingPage
import {{packageName}}.ui.component.NoResultPage
import {{packageName}}.util.Status

@Composable
fun ResourceReactor(status: Status, type: String, success: @Composable () -> Unit) {
    when(status) {
        Status.Init -> {
            InitPage(message = "Init page...")
        }
        Status.Error -> {
            ErrorPage(error = "Error occurred while fetching $type")
        }
        Status.Loading -> {
            LoadingPage(loading = "Loading $type...")
        }
        Status.NoResult -> {
            NoResultPage(warning = "No $type")
        }
        Status.Success -> {
            success()
        }
    }
}